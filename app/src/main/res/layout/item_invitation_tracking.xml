<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="@dimen/card_corner_radius_sm"
    app:cardElevation="@dimen/elevation_none"
    app:cardBackgroundColor="@color/lumo_surface"
    app:strokeColor="@color/lumo_outline_variant"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/card_padding">

        <!-- Row 1: email + status badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <TextView
                android:id="@+id/tvInviteeEmail"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/outfit_semibold"
                android:textSize="14sp"
                android:textColor="@color/lumo_on_surface"
                android:ellipsize="end"
                android:maxLines="1" />

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:fontFamily="@font/outfit_semibold"
                android:textSize="10sp"
                android:letterSpacing="0.05"
                android:textAllCaps="true"
                android:background="@drawable/bg_badge_rounded" />

        </LinearLayout>

        <!-- Row 2: sent date + email opened indicator -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView
                android:id="@+id/tvSentAt"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/outfit"
                android:textSize="12sp"
                android:textColor="@color/lumo_on_surface_variant" />

            <TextView
                android:id="@+id/tvEmailOpened"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="@string/tracking_email_opened"
                android:textColor="@color/lumo_success"
                android:textSize="10sp"
                android:fontFamily="@font/outfit_semibold"
                android:background="@drawable/bg_tracking_email_opened"
                android:visibility="gone" />

        </LinearLayout>

        <!-- Row 3: resend count label (optional) -->
        <TextView
            android:id="@+id/tvResendCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:fontFamily="@font/outfit"
            android:textSize="11sp"
            android:textColor="@color/lumo_on_surface_variant"
            android:visibility="gone" />

        <!-- Row 4: action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnResend"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="@dimen/button_height_alert"
                android:layout_marginEnd="4dp"
                android:text="@string/tracking_btn_resend"
                android:fontFamily="@font/outfit_semibold"
                android:textSize="12sp"
                android:textColor="@color/lumo_primary"
                app:rippleColor="@color/lumo_primary_container"
                android:visibility="gone" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRevoke"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="@dimen/button_height_alert"
                android:text="@string/invite_revoke_action"
                android:fontFamily="@font/outfit_semibold"
                android:textSize="12sp"
                android:textColor="@color/lumo_secondary"
                app:rippleColor="@color/lumo_secondary_container"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
